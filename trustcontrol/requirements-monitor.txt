# ════════════════════════════════════════════════════════════
#  TrustControl — Зависимости воркера (кассовый ПК)
#  Протестировано на Python 3.13 / Windows 10-11 x64
#
#  БЫСТРАЯ УСТАНОВКА (запусти setup.bat двойным кликом):
#    setup.bat
#
#  Ручная установка:
#    py -3.13 -m pip install -r requirements-monitor.txt
#
#  С локальным Whisper (бесплатная транскрипция):
#    py -3.13 -m pip install -r requirements-monitor.txt faster-whisper
# ════════════════════════════════════════════════════════════

# ── Python 3.13: модули удалены из стандартной библиотеки ────
# audioop, aifc, sunau, chunk больше не входят в Python 3.13+
# Эти пакеты возвращают совместимость:
audioop-lts>=0.2.2
standard-aifc>=3.13.0
standard-sunau>=3.13.0
standard-chunk>=3.13.0

# ── Числовые вычисления ──────────────────────────────────────
# ВАЖНО: numpy 1.x не работает с Python 3.13, нужен 2.x
numpy>=2.0.0

# ── Аудио захват ─────────────────────────────────────────────
# pyaudio: для Python 3.13 берётся готовое колесо cp313-win_amd64
pyaudio>=0.2.14
sounddevice>=0.5.0     # резервный аудио-бэкенд

# ── Детектор речи (VAD) ──────────────────────────────────────
# webrtcvad-wheels — форк с готовыми колёсами для Python 3.13
# (оригинальный webrtcvad нужно собирать из исходников)
webrtcvad-wheels>=2.0.10

# ── Шумоподавление ───────────────────────────────────────────
noisereduce>=3.0.2

# ── Отправка на сервер ───────────────────────────────────────
requests>=2.33.0

# ── Опционально: локальная транскрипция (бесплатно) ──────────
# Транскрибирует прямо на кассовом ПК, без OpenAI.
# Запускать monitor.py с флагом --local-whisper
# Раскомментируй строку ниже или установи отдельно:
# faster-whisper>=1.0.0

# ── Опционально: сжатие MP3 64kbps (экономия 75-85% трафика) ─
# Требует ffmpeg в PATH: https://ffmpeg.org/download.html
# После установки ffmpeg раскомментируй строку ниже:
# pydub>=0.25.1
